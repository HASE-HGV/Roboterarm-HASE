[package]
name = "rustctl"
version = "0.1.0"
edition = "2024"

[dependencies]
rppal = { version = "0.22", optional = true }
ctrlc = { version = "3", optional = true }

[features]
default = ["hardware"]
hardware = ["dep:rppal", "dep:ctrlc"]
