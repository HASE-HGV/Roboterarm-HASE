[package]
name = "rustctl"
version = "0.1.0"
edition = "2024"

[features]
default = []
hardware = ["dep:ctrlc", "dep:rppal"]

[target.'cfg(target_os = "linux")'.dependencies]
ctrlc = { version = "3", optional = true }
rppal = { version = "0.22", optional = true }
